version: '3'

services:
  nginx:
    image: nginx:1.19
    ports:
      - 8080:80
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php

  php:
    build: ./docker/php
    ports:
      - 8765:8765
    volumes:
      - ./fivews:/var/www/fivews
